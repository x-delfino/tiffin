name: Packer build

on:
  push:
    branches:
      - linuxupdates

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v4

      - run: echo $PARALLELS_DMG_URL